# Required for signing and verifying JSON Web Tokens.
JWT_SECRET=replace-with-a-generated-secret

# Currently documented for setup; config/db.js is hard-coded to this URI.
MONGODB_URI=mongodb://127.0.0.1:27017/aetickethub

# Currently documented for setup; server.js is hard-coded to 5000.
PORT=5000
